import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Drums - Modern Next.js TypeScript App",
  description: "A beautiful, modern web application built with Next.js 14, TypeScript, and Tailwind CSS. Experience lightning-fast performance and exceptional developer experience.",
  keywords: ["Next.js", "TypeScript", "React", "Tailwind CSS", "Modern Web App"],
  authors: [{ name: "Drums Team" }],
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
